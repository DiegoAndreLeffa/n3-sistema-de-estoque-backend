import { Request, Response } from "express";

export const createProductController = (Request, Response) => {};
export const listProductController = (Request, Response) => {};
export const listIdProductController = (Request, Response) => {};
export const updateProductController = (Request, Response) => {};
export const deleteProductController = (Request, Response) => {};
