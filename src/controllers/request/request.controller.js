import { Request, Response } from "express";

export const createRequestController = (Request, Response) => {};
export const listRequestController = (Request, Response) => {};
export const listIdRequestController = (Request, Response) => {};
export const updateRequestController = (Request, Response) => {};
export const deleteRequestController = (Request, Response) => {};
