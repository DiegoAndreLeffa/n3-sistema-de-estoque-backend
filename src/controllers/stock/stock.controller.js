import { Request, Response } from "express";

export const createStockController = (Request, Response) => {};
export const listStockController = (Request, Response) => {};
export const listIdStockController = (Request, Response) => {};
export const updateStockController = (Request, Response) => {};
export const deleteStockController = (Request, Response) => {};
